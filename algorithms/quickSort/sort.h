#ifndef SORT_H
#define SORT_H

#include <cstdlib>

void swap(int&, int&);
void showArray(int*, int);
int* initArray(int*, int);
int getSize();
int* quickSort(int*, int, int);
int* quick_sort(int*, int, int);

#endif